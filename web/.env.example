# Copiá este archivo a .env.local y completá los valores:
#   cp .env.example .env.local
#
# Next.js carga automáticamente .env.local (no lo subas a git).

# OpenAI — activa respuestas del asistente con modelo cloud (clave en platform.openai.com)
OPENAI_API_KEY=

# Opcional (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
